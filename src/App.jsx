// Ponto de entrada da aplicação. As rotas e features (catalog, booking,
// checkout, account) entram aqui à medida que cada spec é implementada —
// ver a skill frontend-architecture do plugin team-ludens e as specs em
// docs.ludens/specs/.
export function App() {
  return (
    <main>
      <h1>Ludens</h1>
      <p>Ingressos do teatro comunitário.</p>
    </main>
  );
}
